class AppContainer {
    Artists = []
    Eras = []
    url =  "" //(Probably local host 3000)
    discoverArtists = {}
    
    getArtists(){
        //make a fetch request to artists
        //populate artists and eras properties with the returned data
        //then call renderArtists
    }
    renderArtists(){
        //create DOM nodes and the insert data into them 
    }
}

